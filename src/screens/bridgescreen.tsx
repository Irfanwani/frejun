import { FC, memo } from "react";
import { Text } from "react-native";
import styles from "../components/styles";

const BridgeScreen: FC = () => {
    return <Text style={styles.empty}>Coming Soon</Text>
}

export default memo(BridgeScreen)